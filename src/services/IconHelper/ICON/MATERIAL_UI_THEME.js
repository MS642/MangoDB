const themePrefix = "material-icons";

const MATERIAL_UI_THEME = {
  filled: themePrefix,
  outlined: `${themePrefix}-outlined`,
  twotone: `${themePrefix}-two-tone`,
};

export default MATERIAL_UI_THEME;
