import * as React from "react";

class NavBar extends React.Component {
    render() {
        return (
            <div>
                TEMP NavBar
            </div>
        );
    }
}

export default NavBar;